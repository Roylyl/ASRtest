# 本地仓库备份检查
检查日期：2026-09-21。本次将桌面 ASRtest 备份到本地 Git 仓库，保留原目录，未上传或创建远程仓库，未执行 Git 提交。
## 已完成
| 检查 | 结果 |
|---|---|
| 应用源码及资源 | `ASRtest/` 内 15 个文件与桌面原件逐字节一致；识别与界面逻辑未改动。 |
| 模型 | 八组、42 个必要文件，合计 1,857,000,819 字节，大小及 SHA256 全部通过。 |
| 本地运行库 | Whisper、Vosk、Nano 共 71 个文件的备份校验通过，真机及模拟器 arm64 切片、头文件、模块与必要接口符号检查通过。 |
| 恢复脚本 | 从完整备份恢复到空临时目录后通过全量校验；损坏文件可被检测，未明确允许时拒绝覆盖不同目标。 |
| 新目录构建 | 使用新的构建目录编译 iPhone arm64 Debug App 成功，关闭代码签名；未安装到真机。 |
| 工程便携性 | 个人签名移至被忽略的 `Config/Local.xcconfig`，共享配置使用变量与示例；脚本移除个人路径与固定模拟器 ID。 |
| Git 范围 | 模型、XCFramework、Vendor、缓存、运行日志及个人配置被忽略；工程、清单、测试源码与许可文件保留。 |
| 版权资料 | 保存 12 份上游许可与第三方告知原文；来源与校验值位于 `Licenses/sources.json`，未为整个工程新授开源许可。 |
## 验证范围
本次重新验证资源完整性、原包恢复能力、新位置构建和脚本基础行为，没有重新跑全套模型推理、录音或 UI 测试。桌面版本既有模拟器推理与 UI 检查属于历史证据；物理 iPhone 的麦克风、准确率、内存、耗电和温升仍需实际测量。
新增 Whisper 源码重建脚本仅完成语法和命令帮助检查，没有完整执行源码重建；当前 App 编译使用从桌面复制且校验通过的运行库。`--check-structure` 只能证明基本打包结构及接口存在，不能证明新二进制的识别行为一致。
当前本地副本包含被 Git 忽略的大文件。以后单独克隆 Git 源码，需要按 README 恢复模型和运行库，不能把 Git 源码仓库当作完整二进制备份。SenseVoice 权重历史许可对应和 Vosk 社区二进制依赖来源仍有待确认，见 `THIRD_PARTY_NOTICES.md`。
本次原始构建日志和机器路径保留于本地 `LocalBackup/`，不进入 Git。
